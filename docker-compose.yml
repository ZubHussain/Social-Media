version: "3.8"

services:
  mongodb:
    container_name: mongo
    image: mongo:latest
    ports:
      - "27017:27017"

  backend:
    container_name: backend
    build: ./server
    # env_file:
    #   - ./server/.env
    ports:
      - "3000:3000"

  frontend:
    container_name: frontend
    build: ./client
    ports:
      - "5173:5173"
